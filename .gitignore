node_modules
dist
.env
.env.local
.env.*.local
backend/.env
backend/.env.local
.vscode
*.log
*.tsbuildinfo
.next

# Stale duplicate scaffold (Jul 14) kept locally, never tracked.
# It carries its own .env files with real credentials.
nexus-map/
.vercel

# Local backups and tool state — never commit (may contain secrets)
*.bak
.env.local.bak
.freebuff/
.env*
